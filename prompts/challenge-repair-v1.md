Your previous challenge was rejected by automated validation. Fix every problem listed below and return the complete corrected challenge as a single JSON object matching the schema. Keep the same scenario unless a problem makes it impossible; do not simply delete failing tests — make the tests, starter code and reference solution consistent.

## Problems found

{{problems}}
